services:
  postgres:
    image: 'postgres:latest'
    container_name: 'config-service-postgres-test'
    environment:
      - 'POSTGRES_DB=config-service-db'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432:5432'